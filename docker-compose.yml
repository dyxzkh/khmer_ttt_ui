version: '3.8'

services:
  vue-app:
    build: .
    ports:
      - "80:80"
    environment:
      - PORT=80
    volumes:
      - .:/app
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
